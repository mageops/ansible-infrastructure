[Service]
ExecReload=-/bin/bash -c '/usr/sbin/nginx -c {{ nginx_etc_dir }}/nginx.conf -Tq > {{ nginx_etc_dir }}/nginx.conf.dump.loaded'
ExecStartPost=-/bin/bash -c '/usr/sbin/nginx -c {{ nginx_etc_dir }}/nginx.conf -Tq > {{ nginx_etc_dir }}/nginx.conf.dump.loaded'