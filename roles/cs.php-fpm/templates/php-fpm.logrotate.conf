{{ php_fpm_log_dir_path }}/*log {
    missingok
    notifempty
    sharedscripts
    delaycompress
    postrotate
	    /bin/kill -SIGUSR1 `cat {{ php_fpm_run_dir_path }}/*.pid 2>/dev/null` 2>/dev/null || true
    endscript
}